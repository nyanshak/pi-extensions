{
  "name": "pi-acp",
  "version": "0.1.0",
  "description": "ACP (Agent Client Protocol) support for pi-coding-agent",
  "type": "module",
  "pi": {
    "extensions": ["./src/index.ts"]
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^1.0.0"
  },
  "dependencies": {}
}